%h1.orange Listing products

%table.table.table-hover.orange
  %tr
    %th Title
    %th Description
    %th Price
    %th Show
    %th Edit
    %th Delete

  - products.each do |product|
    %tr
      %td= product.title
      %td= product.description
      %td= product.price
      %td
        = link_to category_product_path(product.category_id, product.id) do
          %span.glyphicon.glyphicon-list-alt
      %td
        = link_to edit_category_product_path(product.category_id, product.id) do
          %span.glyphicon.glyphicon-edit
      %td
        = link_to  category_product_path(product.category_id, product.id), method: :delete, data: { confirm: 'Are you sure?' } do
          %span.glyphicon.glyphicon-remove.large
%br
